# Landing Page

> صفحهٔ معرفی محصول میزان — استاتیک، بدون build step.

## مسئولیت‌ها
- `landing/index.html` + `landing/styles.css` — RTL، فارسی.
- بخش‌ها: hero، مسئله، features (۶ کارت)، پرسونا‌ها، contact.
- CTA اصلی: `mailto:hello@mizan.ai` (لینک contact).

## وابستگی‌ها
- مستقل از backend — فقط با باز کردن مستقیم فایل در مرورگر قابل مشاهده است.

## قراردادها / Edge cases
- `hello@mizan.ai` یک **placeholder** است، دامنهٔ واقعی/ایمیل واقعی متصل نیست (نکتهٔ باز — TASKS.md P3-6).

## منابع کد
- `landing/index.html`
- `landing/styles.css`
