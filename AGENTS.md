# AGENTS.md — میزان (Mizan)

> این فایل دستورالعمل نگهداری ویکی دانش پروژه است. هر ایجنتی که روی این ریپو کار می‌کند باید این قوانین را رعایت کند.

---

# Vault Recipe — دستورالعمل ویکی دانش پروژه

## این چیست
یک «ویکی دانش» زنده و append-friendly کنار کد، در `docs/wiki/`، که به‌جای re-scan کردن کل کدبیس در هر مکالمه،
خلاصه‌ی entity ها (مدل‌ها، سرویس‌ها، صفحات) و concept ها (flowها، الگوها، تصمیم‌ها) را نگه می‌دارد.

## ساختار پوشه
```
docs/wiki/
  schema.md      # قوانین کامل ساختار و لینک‌گذاری
  overview.md    # یک‌نگاه کلی پروژه (≤۴۰۰ کلمه)
  index.md       # کاتالوگ همه صفحات، گروه‌بندی‌شده
  log.md         # تاریخچه append-only (جدیدترین در پایین)
  entities/      # هر entity مهم یک صفحه (router، parser، engine، client، model)
  concepts/      # patternها، flowها، تصمیم‌های معماری
  sources/       # (اختیاری) خلاصه فایل‌های خام پرارجاع
```

## شروع هر مکالمه/سشن
1. اول `docs/wiki/overview.md` و `docs/wiki/index.md` را بخوان (≤۸۰۰ توکن). هرگز کل کدبیس را re-scan نکن.
2. فقط صفحات مرتبط با تسک فعلی را drill-down کن (`entities/*.md`, `concepts/*.md`).

## وقتی کد را تغییر دادی — قانون اکید
هر بار که یک فایل پروژه را Write/Edit می‌کنی (خارج از `docs/wiki/` خودش):
1. تشخیص بده این تغییر روی کدام entity/concept اثر می‌گذارد.
2. صفحه‌ی مربوطه را به‌روز کن (فیلد جدید/حذف‌شده، امضای تابع، endpoint جدید، env var جدید، edge case).
3. اگر entity/concept کاملاً جدید و به‌اندازه کافی مهم است، صفحه جدید بساز و در `index.md` لینک بده.
4. یک خط در `log.md` اضافه کن: `## [YYYY-MM-DD] update | <خلاصه تغییر>`.

تغییرات trivial (typo، فرمت، rename بدون تغییر امضا) نیاز به آپدیت ویکی ندارند.
اگر چند فایل در یک مکالمه تغییر کردند، آپدیت‌های ویکی را یک‌بار در پایان مکالمه انجام بده، نه بعد از هر فایل.

## چه وقت صفحه جدید بساز
- فقط اگر ≥۳ جای دیگر قرار است به آن لینک بدهند، یا concept مستقلی با حجم قابل‌توجه است.
- در اکثر موارد به‌جای فایل جدید، صفحه‌ی موجود را آپدیت کن.
- هرگز صفحه‌ی trivial نساز (wrapper یک‌خطی، utility ساده) — در صفحه‌ی پدر یک bullet بنویس.

## ساختار صفحه‌ی entity
```markdown
# {نام}

> یک‌خطی توضیح هدف.

## مسئولیت‌ها
- ...

## وابستگی‌ها
- [[concepts/xxx]] — چرا
- [[entities/yyy]] — چطور

## قراردادها / Edge cases
- ...

## منابع کد
- `path/to/file.py:42` — توضیح
```

## قواعد لینک (Obsidian-style)
- `[[entities/xxx]]` بدون پسوند `.md`.
- نام فایل: lowercase + hyphen.
- wikilinkهای نمونه داخل بک‌تیک/code block لینک واقعی محسوب نمی‌شوند.

## Lint دوره‌ای (وقتی کاربر گفت "ویکی رو بازبینی کن")
1. orphan pages — لینک بده یا حذف کن.
2. broken link — صفحه‌ی گمشده را بساز یا لینک را حذف کن.
3. تناقض بین صفحات — flag کن، بهترین حدس را انتخاب کن، در log ثبت کن.
4. صفحات کهنه — بازخوانی و sync با کد فعلی.

## هم‌گام‌سازی با ویکی گیت‌هاب
این ریپو یک ویکی گیت‌هاب رسمی هم دارد (`github.com/massoudsh/mizan-legal-copilot/wiki`, کلون: `.wiki.git`).
وقتی `docs/wiki/overview.md` یا `docs/wiki/index.md` به‌طور معنادار تغییر کرد، همان تغییرات را در ویکی گیت‌هاب هم منعکس کن
(clone → همان صفحات مقابل را sync کن → commit → push). صفحات جزئی‌تر entities/concepts اختیاری‌اند اگر حجم زیاد شود، اولویت با overview/index/architecture است.

## نکته‌ی نهایی
اگر این قانون رعایت نشود، ویکی stale می‌شود و مکالمات بعدی به اطلاعات غلط می‌رسند.
هدف: کاهش توکن مصرفی با دانش فشرده و به‌روز — نه مستندسازی برای مستندسازی.
